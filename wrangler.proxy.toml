name = "toptechreviews-proxy"
main = "workers/pages-proxy.js"
compatibility_date = "2024-06-01"

# Routes removed — Pages custom domain serves toptechreviews.org directly.
# Keep worker for optional manual attach; do not route apex traffic through proxy.
